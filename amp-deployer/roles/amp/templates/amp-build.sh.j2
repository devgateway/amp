#!/bin/sh
## AMP Build script.
## {{ ansible_managed }}
## TODO: Use variables.

if [ ! -f pom.xml ]
then
  echo "Not a maven project, `pom.xml` not found!"
  exit 2
fi

mvn clean \
    generate-resources \
    process-resources \
    -DserverName=local \
    -Djdbc.db=amp_local \
    -Djdbc.user=amp_user \
    -Djdbc.password=demo1 \
    -Djdbc.port=5432
