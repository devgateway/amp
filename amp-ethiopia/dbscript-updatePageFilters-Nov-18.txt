insert into amp_pages 
(page_name,page_code) 
values ('Regional Fundings','RFS');

create table temp_filter
select amp_filter_id from amp_filters
where filter_name in('Calendar','Currency');

create table temp_pages
select amp_page_id from amp_pages
where page_code='RFS';

insert into amp_page_filters
select amp_page_id,amp_filter_id
from temp_pages cross join temp_filter;

drop table temp_pages;
drop table temp_filter;
