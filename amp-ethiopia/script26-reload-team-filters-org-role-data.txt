# re-load amp-team-page-filters data into changed amp-team-page-filter table

# run ant database before running this script

insert into amp_team_page_filters(team, page, filter)
select amp_team_id, amp_page_id, amp_filter_id
from tmp_amp_team_page_filters;

drop table tmp_amp_team_page_filters;

insert into amp_org_role(activity, organisation, role)
select amp_activity_id, amp_org_id, amp_role_id
from tmp_amp_org_role;

drop table tmp_amp_org_role;



