<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://hibernate.sourceforge.net/hibernate-configuration-3.0.dtd">

<hibernate-configuration>

	<session-factory>

		<!-- Database connection settings -->
		<property name="connection.datasource">java:supportDS</property>
		
		<!-- JDBC connection pool (use the built-in) -->
		<property name="connection.pool_size">2</property>
		<property name="connection.isolation">2</property>
		<!-- SQL dialect -->
		<property name="dialect">
			org.hibernate.dialect.MySQLDialect
		</property>
		<!-- Enable Hibernate's automatic session context management -->
		<property name="current_session_context_class">thread</property>

		<!-- Disable the second-level cache  -->
		<property name="cache.provider_class">
			org.hibernate.cache.NoCacheProvider
		</property>

		<!-- Echo all executed SQL to stdout -->
		<property name="show_sql">false</property>

		<!-- Drop and re-create the database schema on startup -->
		<property name="hbm2ddl.auto">update</property>

		<mapping class="org.dgfundation.amp.support.dbentity.Country" />
		<mapping class="org.dgfundation.amp.support.dbentity.Login" />
		<mapping class="org.dgfundation.amp.support.dbentity.RequestSupport" />
		<mapping class="org.dgfundation.amp.support.dbentity.AmpModules"/>
		<mapping class="org.dgfundation.amp.support.dbentity.OperatingSystem"/>
		<mapping class="org.dgfundation.amp.support.dbentity.AmpVersion"/>
		</session-factory>

</hibernate-configuration>

