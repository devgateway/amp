#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/bash" ]; then
  "$basedir/bash"  "$basedir/../csso/bin/csso" "$@"
  ret=$?
else 
  bash  "$basedir/../csso/bin/csso" "$@"
  ret=$?
fi
exit $ret
