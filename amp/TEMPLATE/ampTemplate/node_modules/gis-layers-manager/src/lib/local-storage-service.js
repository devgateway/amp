var localStorageService = {};
var AMP_LAYERS = 'AMP_LAYERS';
var AMP_LAYERS = 'AMP_LAYERS_VALUES';
localStorageService.saveLayer = function(layer){
	localStorage.setItem(AMP_LAYERS, JSON.stringify(layer));
};