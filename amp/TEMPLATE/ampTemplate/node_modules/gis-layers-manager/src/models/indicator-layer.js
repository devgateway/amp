var _ = require('underscore');
var Backbone = require('backbone');
module.exports = Backbone.Model.extend({
  urlRoot: '/rest/postgis/indicator-layer/',
  defaults: {
	    name: '',
	    unit: '',
	    description: '',
	    numberOfClasses: 0	    	    	
  },
  initialize: function(){    
  },
  
  parse : function(resp, xhr) {
      return resp.data;
  }
});
