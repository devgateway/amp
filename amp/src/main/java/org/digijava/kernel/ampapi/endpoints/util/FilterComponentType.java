package org.digijava.kernel.ampapi.endpoints.util;

/**
 * Enum that describes type of component where can be used the filter
 */
public enum FilterComponentType {
    ALL,
    DASHBOARD,
    REPORTS,
    GIS,
    TAB,
    GPI_REPORTS
};
