// Returns undefined since the document in AmpOnline is never downloaded, its indirectly downloaded via AMP
const RepositoryManager = {
    getFullContentFilePath(content) {
        return undefined;
    }
}
export default RepositoryManager;

