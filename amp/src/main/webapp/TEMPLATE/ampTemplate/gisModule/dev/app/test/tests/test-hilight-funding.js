var assert = require("assert"); // node.js core module
var HilightFundingCollection = require('../../js/amp/data/collections/hilight-funding-collection');

var mockOptions = {
  app: { state: { register: function() {}}}
};


describe('HilightFunding Collection', function() {
  it('stub for testing...', function(){
    assert.ok( 1 === 1, 'stub test' );
  });

});
