package org.digijava.kernel.services;

import org.digijava.kernel.AbstractIntegrationTest;
import org.junit.Test;

/**
 * TODO this test can be uncommented after AmpVersionService is no longer dependent on WebApplicationContext
 * @author Octavian Ciubotaru
 */
public class AmpVersionServiceTest extends AbstractIntegrationTest {

//    @Autowired
//    private AmpVersionService ampVersionService;
//
    @Test
    public void simpleTest() {
//        AmpVersionInfo versionInfo = ampVersionService.getVersionInfo();
//        assertTrue(StringUtils.isNotBlank(versionInfo.getAmpVersion()));
    }
}
