<?xml version="1.0" encoding="UTF-8"?>
<tns:patch closeOnSuccess="false" retryOnFail="true"
           xmlns:tns="http://docs.ampdev.net/schemas/xmlpatcher" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
           xsi:schemaLocation="http://docs.ampdev.net/schemas/xmlpatcher ../doc/xmlpatcher.xsd ">
    <jira>AMP-11949</jira>
    <keyword>Paris Indicators</keyword>
    <author>Medea</author>
    <description>add missing paris indicators</description>
    <trigger type="all">
        <condition  type="tableEmpty">amp_ahsurvey_indicator</condition>
    </trigger>
    <apply>
        <script>
            <lang delimiter=";" type="postgres">
                INSERT INTO amp_ahsurvey_indicator (amp_indicator_id, name,
                total_question, indicator_number, indicator_code, status)
                (select
                (SELECT CASE WHEN (Select max(amp_indicator_id) from
                amp_ahsurvey_indicator) is null THEN 1 ELSE (Select
                (max(amp_indicator_id)+1) from amp_ahsurvey_indicator) END),
                'Proportion of aid flows to the government sector reported on
                government budget', 2, 1, '3', NULL);
                
                INSERT INTO amp_ahsurvey_indicator (amp_indicator_id, name,
                total_question, indicator_number, indicator_code, status)
                (select
                (SELECT CASE WHEN (Select max(amp_indicator_id) from
                amp_ahsurvey_indicator) is null THEN 1 ELSE (Select
                (max(amp_indicator_id)+1) from amp_ahsurvey_indicator) END),
                '% of TC
                flows implemented through coordinated programmes consistent with
                national development strategies', 2, 2, '4', NULL);

                INSERT INTO amp_ahsurvey_indicator (amp_indicator_id, name,
                total_question, indicator_number, indicator_code, status)
                (select
                (SELECT CASE WHEN (Select max(amp_indicator_id) from
                amp_ahsurvey_indicator) is null THEN 1 ELSE (Select
                (max(amp_indicator_id)+1) from amp_ahsurvey_indicator) END),
                'Proportion of aid flows to the public sector using country PFM and
                Proportion of donors using country PFM', 3, 3, '5a', NULL);

                INSERT INTO amp_ahsurvey_indicator (amp_indicator_id, name,
                total_question, indicator_number, indicator_code, status)
                (select
                (SELECT CASE WHEN (Select max(amp_indicator_id) from
                amp_ahsurvey_indicator) is null THEN 1 ELSE (Select
                (max(amp_indicator_id)+1) from amp_ahsurvey_indicator) END),
                'Proportion of aid flows to the public sector using country
                procurement system and Proportion of donors using country
                procurement system', 1, 4, '5b', NULL);

                INSERT INTO amp_ahsurvey_indicator (amp_indicator_id, name,
                total_question, indicator_number, indicator_code, status)
                (select
                (SELECT CASE WHEN (Select max(amp_indicator_id) from
                amp_ahsurvey_indicator) is null THEN 1 ELSE (Select
                (max(amp_indicator_id)+1) from amp_ahsurvey_indicator) END),
                'Number
                of parallel PIUs', 1, 5, '6', NULL);

                INSERT INTO amp_ahsurvey_indicator (amp_indicator_id, name,
                total_question, indicator_number, indicator_code, status)
                (select
                (SELECT CASE WHEN (Select max(amp_indicator_id) from
                amp_ahsurvey_indicator) is null THEN 1 ELSE (Select
                (max(amp_indicator_id)+1) from amp_ahsurvey_indicator) END),
                'Proportion of aid disbursed within the fiscal year it was
                scheduled', 1, 6, '7', NULL);

                INSERT INTO amp_ahsurvey_indicator (amp_indicator_id, name,
                total_question, indicator_number, indicator_code, status)
                (select
                (SELECT CASE WHEN (Select max(amp_indicator_id) from
                amp_ahsurvey_indicator) is null THEN 1 ELSE (Select
                (max(amp_indicator_id)+1) from amp_ahsurvey_indicator) END),
                'Proportion of aid flows provided as programme-based approaches
                (PBA)', 1, 7, '9', NULL);

                INSERT INTO amp_ahsurvey_indicator (amp_indicator_id, name,
                total_question, indicator_number, indicator_code, status)
                (select
                (SELECT CASE WHEN (Select max(amp_indicator_id) from
                amp_ahsurvey_indicator) is null THEN 1 ELSE (Select
                (max(amp_indicator_id)+1) from amp_ahsurvey_indicator) END),
                'Proportion of donor missions that are joint', 1, 8, '10a', NULL);

                INSERT INTO amp_ahsurvey_indicator (amp_indicator_id, name,
                total_question, indicator_number, indicator_code, status)
                (select
                (SELECT CASE WHEN (Select max(amp_indicator_id) from
                amp_ahsurvey_indicator) is null THEN 1 ELSE (Select
                (max(amp_indicator_id)+1) from amp_ahsurvey_indicator) END),
                'Proportion of country analytic work that is joint', 1, 9, '10b',
                NULL);
            </lang>
        </script>
    </apply>
</tns:patch>
