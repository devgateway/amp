<?xml version="1.0" encoding="UTF-8"?>
<tns:patch closeOnSuccess="false" retryOnFail="true"
	xmlns:tns="http://docs.ampdev.net/schemas/xmlpatcher" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://docs.ampdev.net/schemas/xmlpatcher ../doc/xmlpatcher.xsd ">
	<jira>AMP-14167</jira>
	<keyword>Views</keyword>
	<author>Alexandru Artimon</author>
	<description>recreate view, this will always be the last version of the view</description>
	<trigger type="all">
		<condition type="custom">
			<script returnVar="val">
				<lang type="sql">
					SELECT settingsvalue FROM amp_global_settings where settingsname='Recreate the views on the next server restart';
				</lang>
			</script>
 			<test>val.equalsIgnoreCase("true")</test>
		</condition>
	</trigger> 
	<apply>
		<script>
			<lang delimiter=";" type="postgres">
			    	CREATE OR REPLACE VIEW v_executing_agency AS SELECT f.activity AS amp_activity_id, o.name, f.organisation AS amp_org_id, sum(f.percentage) AS percentage FROM amp_org_role f, amp_organisation o, amp_role r WHERE (((f.organisation = o.amp_org_id) AND (f.role = r.amp_role_id)) AND ((r.role_code)::text = 'EA'::text)) GROUP BY f.activity, f.role, f.organisation, o.name ORDER BY f.activity, o.name;;;
        		</lang>
		</script>
	</apply>
</tns:patch>
