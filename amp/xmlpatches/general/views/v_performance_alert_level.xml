<?xml version="1.0" encoding="UTF-8"?>
<tns:patch closeOnSuccess="false" retryOnFail="true"
	xmlns:tns="http://docs.ampdev.net/schemas/xmlpatcher" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://docs.ampdev.net/schemas/xmlpatcher ../../../doc/xmlpatcher.xsd ">
	<jira>AMP-26407</jira>
	<author>Octavian Ciubotaru</author>
	<description>recreate view, this will always be the last version of the view</description>
	<trigger type="all">
		<condition type="custom">
			<script returnVar="val">
				<lang type="sql">
					SELECT settingsvalue FROM amp_global_settings where settingsname='Recreate the views on the next server restart';
				</lang>
			</script>
 			<test>val.equalsIgnoreCase("true")</test>
		</condition>
	</trigger> 
	<apply>
		<script>
			<lang delimiter=";" type="postgres"><![CDATA[
				CREATE OR REPLACE VIEW v_performance_alert_level AS
				  SELECT aav.amp_activity_id,
				    v.name,
				    COALESCE(v.level_code, -999999999) AS level_code
				  FROM amp_activity_version aav
				  LEFT JOIN (
				    SELECT
					  aac.amp_activity_id,
					  acv.category_value       AS name,
					  aac.amp_categoryvalue_id AS level_code
				    FROM amp_activities_categoryvalues aac
					  JOIN amp_category_value acv ON acv.id = aac.amp_categoryvalue_id
					  JOIN amp_category_class acc ON acc.id = acv.amp_category_class_id
				    WHERE acc.keyname = 'performance_alert_level'
				  ) v
				  ON aav.amp_activity_id = v.amp_activity_id;
			]]></lang>
		</script>
	</apply>
</tns:patch>
