<?xml version="1.0" encoding="UTF-8"?>
<tns:patch closeOnSuccess="false" retryOnFail="true"
	xmlns:tns="http://docs.ampdev.net/schemas/xmlpatcher" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://docs.ampdev.net/schemas/xmlpatcher ../doc/xmlpatcher.xsd ">
	<jira>AMP-17153</jira>
	<keyword>Views</keyword>
	<author>Constantin Dolghier</author>
	<description>Tertiary_Sector view for pledges</description>
	<trigger type="all">
		<condition type="custom">
			<script returnVar="val">
				<lang type="sql">
					SELECT settingsvalue FROM amp_global_settings where settingsname='Recreate the views on the next server restart';
				</lang>
			</script>
 			<test>val.equalsIgnoreCase("true")</test>
		</condition>
	</trigger> 
	<apply>
		<script>
			<lang delimiter=";" type="postgres">
				DROP VIEW IF EXISTS v_pledges_tertiary_sectors CASCADE;
			   	CREATE OR REPLACE VIEW v_pledges_tertiary_sectors AS 
					SELECT afps.pledge_id,
    					rs.name AS sectorname,
    					rs.amp_sector_id,
    					sum(afps.sector_percentage) AS sector_percentage
   						FROM amp_funding_pledges_sector afps
     						JOIN all_sectors_with_levels aswl ON afps.amp_sector_id = aswl.amp_sector_id
     						JOIN amp_sector rs ON rs.amp_sector_id = aswl.id0
  					WHERE aswl.sector_config_name = 'Tertiary'
  					GROUP BY afps.pledge_id, rs.amp_sector_id
  					ORDER BY afps.pledge_id;
        	</lang>
		</script>
	</apply>
</tns:patch>
