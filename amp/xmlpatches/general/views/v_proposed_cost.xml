<?xml version="1.0" encoding="UTF-8"?>
<tns:patch closeOnSuccess="false" retryOnFail="true"
	xmlns:tns="http://docs.ampdev.net/schemas/xmlpatcher" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://docs.ampdev.net/schemas/xmlpatcher ../doc/xmlpatcher.xsd ">
	<jira>AMP-14167</jira>
	<keyword>Views</keyword>
	<author>Alexandru Artimon</author>
	<description>recreate view, this will always be the last version of the view</description>
	<trigger type="all">
		<condition type="custom">
			<script returnVar="val">
				<lang type="sql">
					SELECT settingsvalue FROM amp_global_settings where settingsname='Recreate the views on the next server restart';
				</lang>
			</script>
 			<test>val.equalsIgnoreCase("true")</test>
		</condition>
	</trigger> 
	<apply>
		<script>
			<lang delimiter=";" type="postgres">
			    	CREATE OR REPLACE VIEW v_proposed_cost AS
			    	SELECT amp_activity.amp_activity_id, amp_activity.amp_activity_id AS object_id, amp_activity.amp_activity_id AS object_id2, 
			    		amp_funding_amount.amount AS transaction_amount, amp_funding_amount.curr_code AS currency_code, amp_funding_amount.date AS transaction_date 
			    	FROM amp_activity, amp_funding_amount 
			    	WHERE amp_activity.amp_activity_id = amp_funding_amount.amp_activity_id AND 
			    		amp_funding_amount.type = 0 AND
			    		(amp_funding_amount.amount IS NOT NULL) AND (amp_funding_amount.date IS NOT NULL);
        		</lang>
		</script>
	</apply>
</tns:patch>
