<h2><%= lc(:title, :donor => @donor.name) %></h2>

<table>
	<% if @agencies.blank? %>
		<tr><td class="empty"><%= lc(:empty) %></td></tr>
	<% else %>
		<tr>
			<th><%= ll(:donor_agency, :code) %></th>
			<th><%= ll(:donor_agency, :name) %></th>
			<th><%= ll(:donor_agency, :acronym) %></th>		
			<th> </th>		
			<th> </th>		
		</tr>
		<% for agency in @agencies %> 
			<tr>
				<td><%= agency.code %></td>
				<td><%= agency.name %></td>
				<td><%= agency.acronym %></td>
				<td><%= link_to ll(:actions, :edit), edit_agency_path(agency) %></td>
				<td><%= link_to ll(:actions, :delete), agency_path(agency), :method => 'delete', 
						:confirm => lc(:confirm_delete, :agency => agency.name) %>
				</td>
			</tr>
		<% end %>
	<% end %>
</table>

<p><b><%= link_to lc(:new), new_donor_agency_path(@donor) %></b></p>
