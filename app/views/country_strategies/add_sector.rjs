page.insert_html :bottom, :sector_details, :partial => 'sector_detail',
                 :locals => { :sector_detail => @sector_detail, :index => params[:index] }

# FIXME: This doesn't work with the jRails replacement for Prototype and script.aculo.us
#page.delay(1) do 
#	page.visual_effect :highlight, "sector_detail_#{params[:index]}", :endcolor => '#dbe2f1'
#end

page.replace :add_sector_link, :partial => 'add_sector_link', :locals => { :index => (params[:index].to_i + 1) }