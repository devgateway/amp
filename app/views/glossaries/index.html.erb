<h2><%= header lc(:title) %></h2>

<table>
	<tr>
		<th><%= Glossary.human_attribute_name('model') %></th>
		<th><%= Glossary.human_attribute_name('method') %></th>
		<th><%= Glossary.human_attribute_name('locale') %></th>
		<th></th>
	</tr>
	
	<% @glossaries.each do |glossary| %>
		<tr>
			<td><%= glossary.model %></td>
			<td><%= glossary.method %></td>	
			<td><%= glossary.locale %></td>
			<td><%= link_to ll(:actions, :edit), edit_glossary_path(glossary) %></td>		
		</tr>
	<% end %>
</table>

<p><b><%= link_to lc(:new), new_glossary_path %></b>&nbsp;</p>

