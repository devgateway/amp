<div class="contracted_agency">
	<select name="project[contracted_agency_ids][]" class="large">
		<option value=""><%= ll(:options, :prompt) %></option>
		<%= options_from_collection_for_select(Agency.ordered.all, :id, :name, contracted_agency.id) %>
	</select>
		
	<%= link_to_function I18n.t('projects.delete_associated'), "$(this).parents('.contracted_agency').remove();" %>
</div>