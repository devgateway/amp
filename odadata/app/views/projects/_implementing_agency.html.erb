<div class="implementing_agency">
	<select name="project[implementing_agency_ids][]" class="large">
		<option value=""><%= t('.prompt') %></option>
		<%= options_from_collection_for_select(Agency.ordered.all, :id, :name, implementing_agency.id) %>
	</select>

	<%= link_to_function t('.delete_associated'), "$(this).parents('.implementing_agency').remove();" %>
</div>